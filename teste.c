ponteiro de vetor para os auxiliares
0-9 usuario 1-10
tam=5 posição -3
antes de alocar tem que verificar se esta vazio
inicializar todo o vetor
int *a; int i;
guardar os tamanhos em um vetor para se necessário utilizar-los depois
  vetor TAM variavél global inicializa-los com sentinela
contar quantos elementos há em cada vetor
necessário um vetor de contador inicializados com 0, para cada espaço de vetor 
ou
utilizar ums struct{
  int * p;
  int cont;
  int tam;
}item;
inicializar
item *vp(10);
item vp(10);
//-> a=null; 
posicao--;
if(vp[posicao] == null){
vp[posicao] == malloc(tam * sizeof(int));
vetortam[posicao] == tam;
